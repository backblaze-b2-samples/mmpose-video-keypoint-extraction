// Every object this sample writes lives under this prefix. Mirrors the backend
// `settings.sample_prefix` (MMPOSE_PREFIX). Used by the scoped Library explorer
// to describe what it scans; the full-bucket Files explorer ignores it.
export const SAMPLE_PREFIX = "mmpose-video-keypoint-extraction/";
